## 일자

- 날짜: 2026-03-10
- 시간: 45분간
- 발표자: SA 이동균

## 청중

- 현재 에이전트를 잘 모르지만 앞으로 개발해야할 가능성이 높은 서버, 클라이언트 개발자

## 목표

- 내용의 엄밀한 정확성 보다 약간 부정확 하더라도 명확하게 이해할 수 있는 방식으로 설명 (단, 틀린 내용이면 안됨)
- 청중은 해당 발표를 듣고 나면,
- LLM 이 뭔지 이해한다.
- 도구(툴) 이 어떻게 동작하는지 이해한다.
- 에이전트가 뭔지 이해한다.
- MCP 가 뭔지 대략 이해한다.
- 파인튜닝과 RAG 가 뭔지 이해한다.
- 에이전트 관점에서 실시간 데이터 파이프라인의 중요성을 이해한다. (RAG 가 가능하려면 필요하기 때문)

## 페이지 디자인 및 구성

- AWS 리인벤트에서 사용하는 형태의 구성을 따른다.
- 각 페이지에 이미지나 인포그래픽을 넣을 수 있는 부분이 있다면 최대한 반영한다.
- 각 페이지는 꼭 필요한 텍스트만 포함하고 부연 설명은 슬라이드 노트로 포함한다.

## 내용

### LLM 은 다음 토큰을 출력하는 함수라는 점을 설명

- 현재 나와있는 모든 LLM 은 하나 이상의 토큰을 입력받아 다음 토큰 하나를 반환하는 함수라고 볼 수 있다.
- I love you 를 출력하는 과정을 이미지로 형상화해서 보여준다.
- I 라는 텍스트가 3차원 숫자로 토큰화 되고, LLM 에 입력된다.
- 3차원 숫자가 토큰으로 LLM 의 출력으로 나오고, 해당 토큰은 love 라는 텍스트가 된다.
- I love 가 3차원 숫자들로 토큰화되고 LLM 에 입력된다.
- 3차원 숫자가 토큰으로 LLM 의 출력으로 나오고, 해당 토큰은 you 라는 텍스트가 된다.
- 이 과정을 계속 해서 EOS 라는 end of sentence, 즉 문장의 끝을 표시하는 토큰이 나올때까지 출력을 다시 입력으로 보내서 출력한다.
- 이 때 입력되는 내용을 보통 컨텍스트라고 부르며, 컨텍스트의 내용에 따라 LLM 이 출력하는 다음 토큰이 확률적으로 결정된다.
- 따라서 내가 원하는 토큰이 잘 나오게 하려면 컨텍스트를 잘 관리 해야한다.
- 꼭 기억할 점은 LLM 은 입력으로 토큰만 받을 수 있고 출력으로 하나의 토큰을 출력한다는 것이다.

### 도구란 외부의 함수를 호출 할 수 있게 텍스트를 정형화 시킨 것

- 사용자가 LLM 으로 웹 검색을 하고 싶다고 가정하자.
- 나는 웹 검색이 가능한 함수가 있지만, LLM 은 텍스트를 입력받고 텍스트만 출력한다. 이런 상황에서 어떻게 LLM 에게 웹 검색을 시킬 수 있을까?
- LLM 에게 도구가 존재한다는 사실, 도구를 실행할 때 필요한 파라미터(예, query 필드가 포함된 json), 그리고 어떤 경우에 이 도구를 실행해야 하는지를 LLM 이 시작할 때 컨텍스트에 포함시켜서 넣어준다.
- LLM 은 똑똑하기 때문에 실행중 웹 검색 함수 호출이 필요하다고 판단하면 파라미터를 json 형태로 출력한다.
- LLM 이 출력하는 내용을 계속 모니터링 하고 있다가, json 을 출력하면 해당 내용을 파싱해서 함수를 호출 한 뒤에 결과를 컨텍스트 맨 뒤에 붙여넣어주고, EOS 가 나올때 까지 계속 출력한다. (기존 LLM 과 동일)

### 에이전트란 LLM 이 도구를 통해 외부의 리소스를 호출 할 수 있게 구성해둔 어플리케이션

- 에이전트란 LLM 이 이런 도구를 통해 외부의 리소스를 사용해서 계속 출력할 수 있게 만들어둔 어플리케이션이다.
- 우리가 잘 아는 에이전트는 키로, 커서 등의 개발용 에이전트들이 있다.
- 최신 에이전트는 위의 내용을 여러가지 기법을 통해서 좀 더 깔끔하게 처리할 수 있게 래핑 해두었을 뿐, 기본적인 내용은 위의 내용에서 벗어날 수 없다.
- LLM 은 하나 이상의 토큰을 입력받아서 토큰을 출력하는 함수이기 때문

### MCP 는 도구를 LLM 과 분리하여 별도의 서버에 두기 위해, LLM 이 해당 서버를 호출하는 방법을 정의한 프로토콜이다.

- 도구는 일반 함수이다. 함수는 에이전트 어플리케이션이 실행해줘야하기 때문에 결국 에이전트의 언어, 런타임 등에 종속될 수 밖에 없다.
- 또한 도구가 에이전트와 묶여 있기 때문에 에이전트 개발자만이 외부의 도구를 함수로 만들어 호출하는 권한이 있어서 에이전트 개발팀에서 모든 외부 리소스 연동을 직접 해야하는 부담이 있다.
- 도구를 외부 서버로 빼내서 에이전트에서는 도구 주소만 입력해서 http 나 rpc 형태로 쓸 수 있다면, 에이전트 만드는 사람은 LLM 에 집중하면되고, 외부 리소스와 연동하는 부분을 외부 리소스를 담당하는 곳에서 관리할 수 있으니 좋을 것이다.
- MCP 는 이렇게 외부에 정의된 도구를, 어떻게 정보를 가져오고, 어떻게 호출할 수 있는 지 정의한 프로토콜일 뿐이다.
- LLM 은 토큰을 입력받아서 토큰을 출력하고, 도구는 어플리케이션에서 토큰 파싱을 할 뿐이라는 것을 기억하자. MCP 는 클라이언트인 에이전트가 원격에 있는 도구 서버에서 어떻게 정보를 가져오고 도구를 실제로 호출할지 정의한 명세이다.

### 파인튜닝과 RAG 는 하나의 문제를 푸는 다른 방법이다.

- LLM 은 컷오프 날짜라는 것이 있다. 해당 날짜 까지의 데이터만 학습에 사용되었다는 것이다.
- LLM 의 컷오프가 24년 12월 이면 최신 S26 에 대한 정보는 없기 때문에, LLM 에 질문하면 헛소리를 하거나 모른다고 할 것이다.
- 이때 최신의 정보를 LLM 에 제공하는 방법으로 파인튜닝과 RAG 라는 방식을 사용한다.
- 파인튜닝은 LLM 에 직접 데이터를 학습을 시키는 방식이고, LLM 학습에는 GPU 가 필요하고 모델이 클 수록 GPU 도 많이 필요하다.
- 또 학습된 모델은 기존의 모델과 다른 모델이기 때문에 직접 GPU 에 올려서 서빙해야하므로 GPU 서버 비용이 추가로 발생한다. LLM 은 구조상 동시에 처리할 수 있는 양이 많지 않기 때문에 동시접속자에 따라 서버 비용이 많이 든다.
- RAG 는 도구호출과 비슷하다. 외부에 있는 정보가 필요한 경우에 동적으로 외부의 정보를 가져다가 컨텍스트 사이에 끼워넣어 준다. 검색 에이전트에서 최신 상품 목록을 업데이트 하는 등, 데이터가 상당히 잦은 주기로 업데이트 되어야 하는 경우에는 파인튜닝방식으로는 처리가 불가능하므로 RAG 방식이 훨씬 유리하다.
- 둘의 장단점
- 파인튜닝은 LLM 의 지식과 동작을 업데이트 할 수 있기 때문에, 추가적인 컨텍스트 없이 새로운 정보를 바로 출력하여, 프롬프트를 짧고 간결하게 유지할 수 있다.
- RAG 는 컨텍스트에 정보를 추가해서 LLM 에게 동적으로 정보를 주는 만큼, 호출 할때마다 컨텍스트가 커져서 속도나 비용면에서 손해를 보게 된다.

### 에이전트 시대에는 실시간 데이터 파이프라인이 중요하다.

- 현재 에이전트 동작에 필요한 데이터가 자주 업데이트 되는 경우가 많고, 에이전트의 구현이나 서빙이 훨씬 쉽기 때문에, RAG 를 통해 에이전트를 구현하는 것이 일반적이다.
- 특히 LLM 이 똑똑해지면서 사람이 미리 문서를 정리해서 준비해두기보다 에이전트가 동적으로 문서를 찾아서 가져가도록 시스템들이 전환되고 있다.
- 이 때문에 에이전트의 성능은 LLM 의 성능이나 프롬프트보다 AI 가 접근할 수 있는 데이터의 범위와 품질이 훨씬 중요해지고 있다.
- 따라서 고성능의 에이전트를 개발하기 위해서는 에이전트 개발 뿐아니라, 에이전트에 사용할 데이터를 원하는 주기로 업데이트 할 수 있도록 파이프라인을 만들고 관리하는 것이 매우 중요해지고 있으며, 데이터의 양보다 데이터의 품질관리가 중요해지고 있다.
- 요약하면 고품질의 데이터를 적시에 접근할 수 있는 파이프라인이 없으면 에이전트의 성능을 향상시킬 수 없다.

### AWS 는 이 모든 내용을 다 다루고 있는 에이전트 개발 플랫폼 회사이다.

- AWS 에서는 에이전트 개발에 필요한 프레임워크, 에이전트 운영, RAG 를 위한 지식베이스, 실시간으로 데이터를 추가하고 검색할 수 있는 파이프라인을 모두 갖추고 있는 에이전트 개발 플랫폼을 제공하고 있으며,
- 오늘 이후 발표에서는 해당 내용들의 일부를 간단히 설명하려고 한다.
